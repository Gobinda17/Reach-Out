// Shared between app/admin/layout.jsx (the pre-hydration bootstrap script)
// and AdminShell.jsx (the toggle) so the two never drift out of sync.
export const ADMIN_THEME_KEY = "reachOutAdminTheme";
